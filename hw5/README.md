## File Organization

### `seqseq_part2_bs`: 

- based on seqseq_part1, implemented beam search
  - added --beam_size and --length_penalty flags
    - switched translate to beam search

- `translations`:
    - epoch 10, batchsize 32, hidden size 1024, beam size 10
